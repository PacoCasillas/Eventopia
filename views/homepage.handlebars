{{#each allEvents as |event|}}
  <section class="event-box" id="posts-{{event.id}}">
    <section class="event-header">
      <h3 class="event-title">{{event.title}}</h3>
      <h5 class="event-date">From
        {{startDate}}
        To
        {{endDate}}
      </h5>
    </section>
    <section class="event-body">
      <p class="event-details">{{event.description}}</p>
      <p class="event-time">Start at
        {{event.startTime}}
        End at
        {{event.endTime}}</p>
      <p class="event-capacity-cost">Event capacity:
        {{event.capacity}}
        Cost:
        {{event.cost}}</p>
      <p>{{event.location}}</p>
    </section>

    {{! BOTTON TO ATTEND AND LIKE }}
    <div class="btn-attend">
      <button
        class="btn btn-primary"
        id="btn-attend"
        type="button"
        name="button"
      >Attending</button>
    </div>

    <div class="btn-favorite">
      <button
        class="btn btn-primary"
        id="btn-favorite"
        type="button"
        name="button"
      >Favorite</button>
    </div>

  </section>
{{/each}}